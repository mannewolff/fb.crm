import Box from '@mui/material/Box';

/**
 * Platzhalter fuer die Startansicht. In diesem Stand traegt sie nur den leeren Grund —
 * die Ansichten kommen mit den folgenden Paketen.
 */
export default function StartPage() {
  return <Box component="main" sx={{ minHeight: '100vh' }} />;
}
